# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Config
pydantic-settings==2.2.1

# Database (async PostgreSQL)
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0

# Cache — Valkey client
# We use the redis-py library because Valkey is 100% protocol-compatible
# with Redis. No Valkey-specific client is needed.
# hiredis is an optional C extension that speeds up response parsing.
redis[hiredis]==5.0.4

# Search
elasticsearch[async]==8.13.0

# Object storage — Azure Blob Storage
azure-storage-blob==12.22.0

# Message queue
aio-pika==9.4.1

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-xdist==3.6.1
httpx==0.27.0

# Utilities
python-multipart==0.0.9
